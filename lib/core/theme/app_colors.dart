import 'package:flutter/material.dart';

class AppColors {
  // Cor Primária - Fundo
  static const Color primary = Color(0xFF5a1b2c);

  // Cor Secundária - Botões
  static const Color secondary = Color(0xFFeabb47);

  // Cor Discord
  static const Color discord = Color(0xFF5868f1);

  // Cores de card
  static const Color card = Color.fromARGB(255, 37, 11, 18);
  static const Color card2 = Color.fromARGB(255, 52, 15, 25);

  // Cores Neutras
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF808080);

  // Cores de Feedback
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFf44336);
  static const Color warning = Color(0xFFff9800);
  static const Color info = Color(0xFF2196F3);
}
